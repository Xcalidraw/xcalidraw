# JSON Schema

The Xcalidraw data format uses plaintext JSON.

## Xcalidraw files

When saving an Xcalidraw scene locally to a file, the JSON file (`.xcalidraw`) is using the below format.

### Attributes

| Attribute | Description | Value |
| --- | --- | --- |
| `type` | The type of the Xcalidraw schema | `"xcalidraw"` |
| `version` | The version of the Xcalidraw schema | number |
| `source` | The source URL of the Xcalidraw application | `"https://xcalidraw.com"` |
| `elements` | An array of objects representing xcalidraw elements on canvas | Array containing xcalidraw element objects |
| `appState` | Additional application state/configuration | Object containing application state properties |
| `files` | Data for xcalidraw `image` elements | Object containing image data |

### JSON Schema example

```json
{
  // schema information
  "type": "xcalidraw",
  "version": 2,
  "source": "https://xcalidraw.com",

  // elements on canvas
  "elements": [
    // example element
    {
      "id": "pologsyG-tAraPgiN9xP9b",
      "type": "rectangle",
      "x": 928,
      "y": 319,
      "width": 134,
      "height": 90
      /* ...other element properties */
    }
    /* other elements */
  ],

  // editor state (canvas config, preferences, ...)
  "appState": {
    "gridSize": 20,
    "viewBackgroundColor": "#ffffff"
  },

  // files data for "image" elements, using format `{ [fileId]: fileData }`
  "files": {
    // example of an image data object
    "3cebd7720911620a3938ce77243696149da03861": {
      "mimeType": "image/png",
      "id": "3cebd7720911620a3938c.77243626149da03861",
      "dataURL": "data:image/png;base64,iVBORWOKGgoAAAANSUhEUgA=",
      "created": 1690295874454,
      "lastRetrieved": 1690295874454
    }
    /* ...other image data objects */
  }
}
```

## Xcalidraw clipboard format

When copying selected xcalidraw elements to clipboard, the JSON schema is similar to `.xcalidraw` format, except it differs in attributes.

### Attributes

| Attribute | Description | Example Value |
| --- | --- | --- |
| `type` | The type of the Xcalidraw document. | "xcalidraw/clipboard" |
| `elements` | An array of objects representing xcalidraw elements on canvas. | Array containing xcalidraw element objects (see example below) |
| `files` | Data for xcalidraw `image` elements. | Object containing image data |
